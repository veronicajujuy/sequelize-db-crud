<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <% console.log(movies) %>
    <h1>Movies</h1>
    <ol>
      <% movies.map(movie => { %>
      <li><%= movie.dataValues.title %></li>
      <% }) %>
    </ol>
  </body>
</html>
