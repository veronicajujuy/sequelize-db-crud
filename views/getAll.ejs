<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Movies</h1>
    <ul>
      <% movies.map(movie => { %>
      <li><a href="/movies/<%= movie.id %>"><%= movie.title %></a></li>
      <% }) %>
    </ul>
    <a href="/movies/create"><button>Crear</button></a>
  </body>
</html>
